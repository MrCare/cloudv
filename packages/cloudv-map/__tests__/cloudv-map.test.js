
describe("cloudvMap.test.js", () => {
  it("just for test", () => {
    // expect(wrapper.html()).toMatchSnapshot();
    const nonsense = 'just for test'
    expect(nonsense).toContain(nonsense);
  });
});
